//로직을 짜야함
//카멜표기법:대문자작성.띄어쓰기대신임(ex.hiImIronMan)

function appStart() {
  const handleKeydown = (event) => {
    const key = event.key;
    const keyCode = event.keyCode;
    //document.querySelector('bord')
  };
  window.addEventListener("keydown", handleKeydown);
}

appStart();
